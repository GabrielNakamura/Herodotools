% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/calc_harmonic_mean.R
\name{calc_harmonic_mean}
\alias{calc_harmonic_mean}
\title{Calculate the Harmonic Mean}
\usage{
calc_harmonic_mean(x, na.rm = FALSE, ignore.zero = FALSE)
}
\arguments{
\item{x}{A numeric vector.}

\item{na.rm}{Logical; if \code{TRUE}, remove \code{NA} values before computation.}

\item{ignore.zero}{Logical; if \code{TRUE}, remove zeros before computation.}
}
\value{
A numeric value representing the harmonic mean.
}
\description{
Computes the harmonic mean of a numeric vector.
The harmonic mean is defined as \eqn{n / \sum (1/x_i)},
where \eqn{n} is the number of observations.
}
\details{
\itemize{
\item If \code{ignore.zero = FALSE} and the vector contains zeros,
the harmonic mean is zero by definition.
\item If all values are removed (due to \code{NA}s or zeros),
the function returns \code{NA}.
}
}
\examples{
# Simple harmonic mean
calc_harmonic_mean(c(1, 2, 3))

# With a zero (harmonic mean collapses to 0)
calc_harmonic_mean(c(1, 2, 0))

# Ignoring zeros
calc_harmonic_mean(c(1, 2, 0), ignore.zero = TRUE)

# With NA values
calc_harmonic_mean(c(1, 2, NA), na.rm = TRUE)

# Combining both options
calc_harmonic_mean(c(1, 2, 0, NA), na.rm = TRUE, ignore.zero = TRUE)

}
