# Averaged rates of life mode for Sigmodontinae assemblages

Data frame with three columns with averaged values of tip based metrics
of trait evolution

## Usage

``` r
averaged_rates
```

## Format

An object of class `tbl_df` (inherits from `tbl`, `data.frame`) with
1770 rows and 3 columns.
